python script container needs to be started with --net=host so that container knows to use host network environment

i.e. my local computer 127.0.0.1 in connection string which is the port my postgres container is mapped to on the host 